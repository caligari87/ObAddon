#include "zcommon.acs"

Script "IsMyAssGrass" (void)
{
	str flatz[4] =
	{
		"GRASS1",
		"GRASS2",
		"RROCK19",
		"RROCK20",
	};

	bool onGrass = false;

	for ( int flat_index = 1; flat_index <= 4; flat_index++ )
	{
		if (CheckActorFloorTexture(0, flatz[flat_index]))
		{
			onGrass = true;
		}
	}

	if (onGrass == true)
	{
		SetResultValue(1);
		terminate;
	}
	SetResultValue(0);
}